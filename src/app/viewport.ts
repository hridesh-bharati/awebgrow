// src/app/viewport.ts
import { Viewport } from 'next'

export const viewport: Viewport = {
  themeColor: '#00378a',
  colorScheme: 'light',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
  viewportFit: 'cover',
}